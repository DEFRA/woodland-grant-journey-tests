{
  "name": "woodland-grant-journey-tests",
  "version": "0.0.0",
  "description": "",
  "engines": {
    "node": ">=22.13.1"
  },
  "author": "Defra DDTS",
  "license": "OGL-UK-3.0",
  "scripts": {
    "test": "playwright test --config=playwright.config.js --grep @cdp",
    "test:local": "playwright test --config=playwright.local.config.js --grep @runme",
    "test:ci": "playwright test --config=playwright.ci.config.js --grep @ci",
    "report:publish": "./bin/publish-tests.sh"
  },
  "dependencies": {
    "@axe-core/playwright": "4.11.1",
    "@playwright/test": "1.59.1",
    "ajv": "8.18.0",
    "jsonwebtoken": "9.0.3",
    "mockserver-client": "5.15.0"
  }
}
