{
  "name": "woodland-grant-journey-tests",
  "version": "0.0.0",
  "description": "",
  "engines": {
    "node": ">=22.13.1"
  },
  "author": "Defra DDTS",
  "license": "OGL-UK-3.0",
  "scripts": {
    "test": "playwright test --config=playwright.config.js",
    "test:local": "playwright test --config=playwright.local.config.js",
    "test:ci": "playwright test --config=playwright.ci.config.js",
    "report:publish": "./bin/publish-tests.sh"
  },
  "dependencies": {
    "@playwright/test": "1.52.0"
  }
}
